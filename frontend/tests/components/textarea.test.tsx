import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'

describe('Textarea Component (COMP-04)', () => {
  it.todo('renders textarea element')
  it.todo('has consistent focus states with Input')
  it.todo('supports multi-line input')
  it.todo('applies disabled styling')
})
